# ============================================
# REQUIRED - Application will not start without these
# ============================================

# Database (Required)
# Railway automatically provides this when you add PostgreSQL
# For local development, use your local PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/integration_hub

# Session Secret (Required)
# Generate a random secure string for production
# You can use: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-secure-random-string

# ============================================
# OPTIONAL - App runs without these but with limited features
# ============================================

# OpenAI (Optional - enables smart field mapping)
# Without this, manual field mapping still works
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Stripe (Optional - enables payment processing)
# Without these, subscription features won't work
# Get your keys from: https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_...
# VITE_STRIPE_PUBLIC_KEY=pk_test_...

# ============================================
# DEPLOYMENT - Railway sets these automatically
# ============================================

# NODE_ENV - Railway sets to 'production' automatically
# For local development only, use:
NODE_ENV=development

# PORT - Railway provides this automatically, DO NOT SET
# Local development defaults to 5000

# ============================================
# GITHUB SETUP (Only for repository creation script)
# ============================================

# These are ONLY needed if you run scripts/setup-github.ts
# Not required for application runtime
# Automatically provided by Replit GitHub connection
# REPLIT_CONNECTORS_HOSTNAME=...
# REPL_IDENTITY=...
